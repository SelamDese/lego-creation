domBuilder.appendInputForm()
let legoBtn = document.querySelector(".lego__save")
legoBtn.addEventListener("click", eventListener.handelFormSubmition)
// data.getColors()
//     .then(arrayofColors=>{
//     arrayofColors.forEach(color => {
//         htmlOptionTag +=`<select value=${color.id}>${color.name}</select>`
        // htmlOptionTag +=`<option value=${color.id}>${color.name}</option>`
    // })
// })
